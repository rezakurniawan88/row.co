export type AddressProps = {
    id: number;
    userId: number;
    name: string;
    address: string;
    email: string;
    city: string;
    country: string;
    mobileNumber: string | number;
    zip: string | number;
}